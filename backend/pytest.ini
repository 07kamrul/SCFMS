[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast, isolated tests
    integration: tests that hit the database or full app
addopts = -q
